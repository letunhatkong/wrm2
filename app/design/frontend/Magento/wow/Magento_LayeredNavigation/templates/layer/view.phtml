<?php
/**
 * Copyright © 2016 Magento. All rights reserved.
 * See COPYING.txt for license details.
 */

// @codingStandardsIgnoreFile

?>
<?php
/**
 * Category layered navigation
 *
 * @var $block \Magento\LayeredNavigation\Block\Navigation
 */
?>

<?php
$urlInterface = \Magento\Framework\App\ObjectManager::getInstance()->get('Magento\Framework\UrlInterface');
$curUrl = explode("?", $urlInterface->getCurrentUrl())[0];
?>

<?php if ($block->canShowBlock()): ?>
    <div class="block filter">

        <div class="block-content filter-content">
            <?php echo $block->getChildHtml('state') ?>

            <?php if ($block->getLayer()->getState()->getFilters()): ?>

            <?php endif; ?>
            <?php $wrapOptions = false; ?>
            <?php foreach ($block->getFilters() as $filter): ?>
        <?php if (!$wrapOptions): ?>
            <strong role="heading" aria-level="2"
                    class="block-subtitle filter-subtitle"><?= __('Shopping Options') ?></strong>
            <dl class="filter-options" id="narrow-by-list">

                <div class="filter-list move-sort" id="filter-price">
                    <label class="sel no-icon-mobile checkbox">
                        <a class="filter-choose-sort" href="javascript:void(0);"><span class="123">SORT BY</span>
                            <span class="filter-sign"></span></a>
                    </label><br>
                    <div class="filter-box-sort">
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_order=price'?>"><span class="price">Price:Low to Hight</span></a>
                        </li>
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_order=price&product_list_dir=desc'?>"><span class="price">Price:Hight to Low</span></a>
                        </li>
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_dir=asc'?>"><span class="price">A to Z</span></a>
                        </li>
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_order=name&product_list_dir=desc'?>"><span class="price">Z to A</span></a>
                        </li>
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_dir=asc'?>"><span class="price">Newest to Oldest</span></a>
                        </li>
                        <li class="item">
                            <a href="<?= $curUrl .'?product_list_dir=asc'?>"><span class="price">Oldest to Newest</span></a>
                        </li>
                    </div>
                </div>
                <div class="filter-text">
                    <span>FILTER BY</span>
                </div>




                <?php $wrapOptions = true;
                endif; ?>
                <?php if ($filter->getItemsCount()): ?>
                    <?= $block->getChildBlock('renderer')->render($filter); ?>
                <?php endif; ?>
                <?php endforeach; ?>
                <?php if ($wrapOptions): ?>
            </dl>
        <?php endif; ?>

        </div>
    </div>
    <script type="text/javascript">
        require(['jquery', 'jquery/ui'], function ($) {
            $(document).ready(function ($) {
//    dropdown color
                $('.filter-list .filter-choose-color').click(function () {
                    var isOpen = $('.filter-list .filter-sign').hasClass("open");
                    if (isOpen) {
                        $('.filter-choose-color .filter-sign').removeClass("open");
                        $('.filter-list .filter-box-color').removeClass("active");
                    } else {
                        $('.filter-choose-color .filter-sign').removeClass('open');
                        $('.filter-choose-color .filter-sign').addClass("open");
                        $('.filter-list .filter-box-color').addClass("active");
                    }
                });
//    dropdown price
                $('.filter-list .filter-choose-price').click(function () {
                    var isOpen = $('.filter-list .filter-sign').hasClass("open");
                    if (isOpen) {
                        $('.filter-choose-price .filter-sign').removeClass("open");
                        $('.filter-list .filter-box-price').removeClass("active");
                    } else {
                        $('.filter-choose-price .filter-sign').removeClass('open');
                        $('.filter-choose-price .filter-sign').addClass("open");
                        $('.filter-list .filter-box-price').addClass("active");
                    }
                });

//    dropdown sort
                $('.filter-list .filter-choose-sort').click(function () {
                    var isOpen = $('.filter-list .filter-sign').hasClass("open");
                    if (isOpen) {
                        $('.filter-choose-sort .filter-sign').removeClass("open");
                        $('.filter-list .filter-box-sort').removeClass("active");
                    } else {
                        $('.filter-choose-sort .filter-sign').removeClass('open');
                        $('.filter-choose-sort .filter-sign').addClass("open");
                        $('.filter-list .filter-box-sort').addClass("active");
                    }
                });
            });
        });
    </script>


<?php endif; ?>





